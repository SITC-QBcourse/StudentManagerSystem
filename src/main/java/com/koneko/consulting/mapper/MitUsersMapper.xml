<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.koneko.consulting.mapper.MitUsersMapper">
    <resultMap id="mitUsersMap" type="com.koneko.consulting.pojo.MITUser">
    <id property="userId" column="user_id"/>
    <result property="userName" column="user_name"/>
    <result property="userEmail" column="user_email"/>
    <result property="userPassword" column="user_password"/>
    <result property="userPrivilege" column="user_privilege"/>
    <result property="createdAt" column="created_at"/>
    <result property="updatedAt" column="updated_at"/>
</resultMap>
  <insert id="insertUser" parameterType="com.koneko.consulting.mapper.MitUsersMapper">
    INSERT INTO mit_user (user_id,user_name, user_email, user_password, user_privilege)
    VALUES (#{userId}, #{userName}, #{userEmail}, #{userPassword}, #{userPrivilege})
  </insert>

  <select id="selectUserByEmail" parameterType="string" resultMap="mitUsersMap">
    select user_id, user_name, user_email, user_password, user_privilege
    from mit_user
    where user_email = #{userEmail}
  </select>
</mapper>